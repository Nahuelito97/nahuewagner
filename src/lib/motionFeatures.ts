// Features de framer-motion cargadas async por LazyMotion (App.tsx).
// domMax incluye animaciones de layout, que usa Portfolio (AnimatePresence popLayout).
export { domMax as default } from 'framer-motion';
